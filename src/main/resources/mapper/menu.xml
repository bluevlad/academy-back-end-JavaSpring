<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.academy.mapper.MenuMapper">

    <!-- 메뉴 관리 -->
    <select id="getMenuTree" resultType="org.json.simple.JSONObject">
        SELECT
            MENU_ID, MENU_NM, MENU_SEQ, MENU_URL, P_MENUID, ISUSE, TARGET, MENU_INFO
        FROM TB_SG_MENU_MST
        ORDER BY MENU_SEQ, MENU_ID
    </select>

    <select id="getpassMenuTree" resultType="org.json.simple.JSONObject">
        SELECT
            MENU_ID, MENU_NM, MENU_SEQ, MENU_URL, P_MENUID, ISUSE, TARGET, MENU_INFO
        FROM TB_SG_MENU_MST2
        ORDER BY MENU_SEQ, MENU_ID
    </select>

    <!-- 메뉴 상세정보 -->
    <select id="getDetailMenu" parameterType="com.academy.menu.service.MenuVO" resultType="hashMap">
        SELECT
            ONOFF_DIV, MENU_ID, MENU_NM, MENU_SEQ, MENU_URL, P_MENUID, ISUSE, TARGET, MENU_INFO
        FROM TB_SG_MENU_MST
        WHERE MENU_ID = #{menuId}
    </select>

    <select id="getpassDetailMenu" parameterType="com.academy.menu.service.MenuVO" resultType="hashMap">
        SELECT
            ONOFF_DIV, MENU_ID, MENU_NM, MENU_SEQ, MENU_URL
            , P_MENUID, ISUSE, TARGET, MENU_INFO, CODE
            , TOP_IMG_URL, LEFT_IMG_URL, TITL_IMG_URL, SUBTITL_IMG_URL
        FROM TB_SG_MENU_MST2
        WHERE MENU_ID = #{menuId}
    </select>

    <update id="menuUpdateProcess" parameterType="com.academy.menu.service.MenuVO">
        UPDATE TB_SG_MENU_MST
        SET
            MENU_NM = #{menuNm},
            MENU_SEQ = #{menuSeq},
            MENU_URL = #{menuUrl},
            P_MENUID = #{pMenuid},
            ISUSE = #{isuse},
            TARGET = #{target},
            MENU_INFO = #{menuInfo},
            UPD_ID = #{updId},
            UPD_DT = NOW()
        WHERE MENU_ID = #{menuId}
    </update>

    <update id="passmenuUpdateProcess" parameterType="com.academy.menu.service.MenuVO">
        UPDATE TB_SG_MENU_MST2
        SET
            MENU_NM = #{menuNm},
            MENU_SEQ = #{menuSeq},
            MENU_URL = #{menuUrl},
            P_MENUID = #{pMenuid},
            ISUSE = #{isuse},
            TARGET = #{target},
            MENU_INFO = #{menuInfo},
            CODE = #{code},
            TOP_IMG_URL = #{topImgUrl},
            LEFT_IMG_URL = #{leftImgUrl},
            TITL_IMG_URL = #{titlImgUrl},
            SUBTITL_IMG_URL = #{subtitlImgUrl},
            UPD_ID = #{updId},
            UPD_DT = NOW()
        WHERE MENU_ID = #{menuId}
    </update>

    <delete id="menuDeleteProcess" parameterType="com.academy.menu.service.MenuVO">
        DELETE FROM TB_SG_MENU_MST WHERE MENU_ID = #{menuId}
    </delete>

    <delete id="passmenuDeleteProcess" parameterType="com.academy.menu.service.MenuVO">
        DELETE FROM TB_SG_MENU_MST2 WHERE MENU_ID = #{menuId}
    </delete>

    <select id="menuIdCheck" parameterType="com.academy.menu.service.MenuVO" resultType="int">
        SELECT
            COUNT(*) CNT
        FROM TB_SG_MENU_MST
        WHERE MENU_ID = #{menuId}
    </select>

    <select id="getMaxMenuId" parameterType="com.academy.menu.service.MenuVO" resultType="hashMap">
        SELECT
            CONCAT(SUBSTRING(MAX(MENU_ID), 1, LENGTH(MAX(MENU_ID)) - 3), LPAD(SUBSTR(MAX(MENU_ID), -3)+1, 3, '0')) MENU_ID,
            CONCAT(SUBSTRING(MAX(MENU_SEQ), 1, LENGTH(MAX(MENU_SEQ)) - 3), LPAD(SUBSTR(MAX(MENU_SEQ), -3)+1, 3, '0')) MENU_SEQ
        FROM TB_SG_MENU_MST
        WHERE P_MENUID=#{menuId}
    </select>

    <select id="getpassMaxMenuId" parameterType="com.academy.menu.service.MenuVO" resultType="hashMap">
        SELECT
            CONCAT(SUBSTRING(MAX(MENU_ID), 1, LENGTH(MAX(MENU_ID)) - 3), LPAD(SUBSTR(MAX(MENU_ID), -3)+1, 3, '0')) MENU_ID,
            CONCAT(SUBSTRING(MAX(MENU_SEQ), 1, LENGTH(MAX(MENU_SEQ)) - 3), LPAD(SUBSTR(MAX(MENU_SEQ), -3)+1, 3, '0')) MENU_SEQ
        FROM TB_SG_MENU_MST2
        WHERE P_MENUID=#{menuId}
    </select>

    <insert id="menuInsertProcess" parameterType="com.academy.menu.service.MenuVO">
        INSERT INTO TB_SG_MENU_MST(
            ONOFF_DIV,
            MENU_ID,
            MENU_NM,
            MENU_SEQ,
            MENU_URL,
            P_MENUID,
            ISUSE,
            TARGET,
            MENU_INFO,
            REG_ID,
            REG_DT
        )VALUES(
            #{onoffDiv},
            #{menuId},
            #{menuNm},
            #{menuSeq},
            #{menuUrl},
            #{pMenuid},
            #{isuse},
            #{target},
            #{menuInfo},
            #{regId},
            NOW()
        )
    </insert>

    <insert id="passmenuInsertProcess" parameterType="com.academy.menu.service.MenuVO">
        INSERT INTO TB_SG_MENU_MST2(
            ONOFF_DIV,
            MENU_ID,
            MENU_NM,
            MENU_SEQ,
            MENU_URL,
            P_MENUID,
            ISUSE,
            TARGET,
            MENU_INFO,
            CODE,
            TOP_IMG_URL,
            LEFT_IMG_URL,
            TITL_IMG_URL,
            SUBTITL_IMG_URL,
            REG_ID,
            REG_DT
        )VALUES(
            #{onoffDiv},
            #{menuId},
            #{menuNm},
            #{menuSeq},
            #{menuUrl},
            #{pMenuid},
            #{isuse},
            #{target},
            #{menuInfo},
            #{code},
            #{topImgUrl},
            #{leftImgUrl},
            #{titlImgUrl},
            #{subtitlImgUrl},
            #{regId},
            NOW()
        )
    </insert>

</mapper>
